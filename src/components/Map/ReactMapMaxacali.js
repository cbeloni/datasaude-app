import React from "react";

// Componente placeholder para evitar erro de import durante o build.
// Substitua pelo componente real do Maxacali quando disponível.
const ReactMapMaxacali = () => {
  return (
    <div style={{ padding: 16 }}>
      <h3>Mapa Maxacali</h3>
      <p>Componente temporário — implementar visualização do Maxacali aqui.</p>
    </div>
  );
};

export default ReactMapMaxacali;
